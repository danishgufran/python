# Python program to shuffle a deck of card
# importing modules
import random
colour=['red','black']
everything=['Spade','Heart','Diamond','Club','king','queen','joker','ace']
special=['king','queen','jack','ace']
# make a deck of cards
print("You got:")
for i in range(5):
 number = random.randint(2,10)
 color=random.choice(colour)
# shuffle the cards
 deck=random.choice(everything)
 if deck in special:
   print(deck,'\t\t(',color,')')
 else:
   print(number, "of", deck,'\t\t(',color,')')
